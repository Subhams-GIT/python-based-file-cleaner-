[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "filecleanup"
version = "0.1.0"
description = "Smart file cleanup CLI"
requires-python = ">=3.10"

[project.scripts]
filecleanup = "filecleanup.main:main"
